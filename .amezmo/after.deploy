#!/bin/bash

# Ensure our script fails if the apply command returns non-zero (failure exit code)
set -e; 

# Make sure the script is executable
chmod +x project-config/apply;

# Execute our script
./project-config/apply;
